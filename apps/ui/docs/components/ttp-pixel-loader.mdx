# Pixel Loader

A retro-inspired pixel art loading animation that brings a nostalgic feel to your application.

## Features

- 🎮 **Retro Style** - Pixel art aesthetic
- ⚡ **Lightweight** - Minimal performance impact
- 🎨 **Customizable** - Easy to theme and modify
- 📱 **Responsive** - Works on all screen sizes

## Installation

```bash
npm install motion
```

## Usage

```tsx
import { PixelLoader } from '@/components/ui/totheprod-ui/ttp-pixel-loader'

export const LoadingPage = () => {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <PixelLoader />
    </div>
  )
}
```

## Props

| Prop | Type | Default | Required | Description |
|------|------|---------|----------|-------------|
| `rows` | `number | undefined` | - | ❌ | The number of rows to display (default is 25) |
| `columns` | `number | undefined` | - | ❌ | The number of columns to display (default is 25) |
| `duration` | `number | undefined` | - | ❌ | The duration of the animation (default is 0.1 seconds) |
| `delay` | `number | undefined` | - | ❌ | The delay of the animation (default is 0.5 seconds) |
| `randomness` | `number | undefined` | - | ❌ | The amount of randomness to apply to the circumference (default is 0.3, which is ±15%) |
 Type   | Default   | Description                  |
| ----- | ------ | --------- | ---------------------------- |
| size  | number | 40        | Size of the loader in pixels |
| color | string | 'primary' | Color theme of the loader    |

## Examples

### Basic Usage

```tsx
<PixelLoader />
```

### Custom Size

```tsx
<PixelLoader size={60} />
```

### With Custom Color

```tsx
<PixelLoader color="#00ff00" />
```

## Use Cases

Perfect for:

- Game-style applications
- Retro-themed websites
- Loading states in creative projects
- Splash screens
