#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "⚙️  Generating component props..."
cd apps/ui && bun run extract:props:all
if [ $? -ne 0 ]; then
  echo "❌ Props generation failed. Please check the components and try again."
  exit 1
fi
cd ../..

echo "📦 Staging generated props files..."
git add apps/ui/content/props/*.json
git add apps/ui/public/content/props/*.json

echo "✅ Props generated and staged!"
